console.error('Static demo seeding is disabled because reusable demo credentials must not be committed.');
console.error('Use npm run seed:admin with SEED_ADMIN_PASSWORD for a controlled temporary admin account.');
process.exit(1);
