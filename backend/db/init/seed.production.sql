-- Production demo seeding is intentionally disabled.
-- Provision production accounts through controlled admin tooling, never from
-- committed reusable credentials or password hashes.
SELECT 'seed.production.sql disabled; use controlled admin provisioning' AS message;
