-- Static demo seed SQL is intentionally disabled.
-- Use `npm run seed` with SEED_DEMO_PASSWORD set so no reusable credential
-- or password hash is committed to source control.
SELECT 'seed.sql disabled; use backend/scripts/seed.js' AS message;
