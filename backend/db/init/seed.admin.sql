-- Static admin seed SQL is intentionally disabled.
-- Use `npm run seed:admin` with SEED_ADMIN_PASSWORD set so no reusable
-- credential or password hash is committed to source control.
SELECT 'seed.admin.sql disabled; use backend/scripts/seed-admin.js' AS message;
