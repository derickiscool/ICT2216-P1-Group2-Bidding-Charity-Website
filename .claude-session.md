# SESSION_SNAPSHOT v1 | BidForGood ICT2216-P1-G2

## STACK
BE=Express+TS :5000 ts-node-dev; FE=React+Vite :5173; DB=PG Docker :5432 db=bidforgood user=postgres pw=your_password_here; WS=socket.io; monorepo=npm workspaces

## SCHEMA_ADDITIONS(this session)
users: +charity_id BIGINT FK→charities ON DELETE SET NULL, +last_login_at TIMESTAMPTZ, +contact_number TEXT
campaigns: NEW TABLE(id,uuid,charity_id FK→charities,name,description,status CHECK(active/closed),end_date DATE,image_data BYTEA,image_mime TEXT,created_at)
total_raised+active_auctions=computed via SQL subquery NOT stored

## FR05_STAFF(merged PR#14 feat/FR05-backend)
NEW: backend/src/services/charityStaff.service.ts — resolveOwnedCharity(→getCharityByOwnerUserId)+requireApprovedCharity; CRUD via addUser(charityId,roles=['charity_staff'])+updateUser+is_active=false
NEW: backend/src/controllers/charityStaff.controller.ts
ROUTES in charity.routes.ts: GET/POST /staff, PUT/PATCH /staff/:uuid/deactivate — auth+csrf+requireRole(charity,admin)
FE: CharityStaffManagementPage.tsx — useEffect fetch, FormData create/edit, uuid-keyed state; canManageStaff from API not user.is_verified
NEW repo methods: findUserByUsername, listStaffByCharityId, getCharityByOwnerUserId, getCharityById

## FR06_CAMPAIGNS(merged PR#20 feat/FR06-backend)
NEW: backend/src/services/campaign.service.ts — resolveManageableCharity(handles charity+charity_staff roles), requireApproved, detectImageMime(magic bytes), processUploadedImage(2MB,JPG/PNG/WEBP)
NEW: backend/src/controllers/campaign.controller.ts — getCampaigns,postCampaign,putCampaign,patchCloseCampaign,getCampaignImage(streams BYTEA)
ROUTES: GET/POST /campaigns, PUT/PATCH /campaigns/:uuid, GET /campaigns/:uuid/image(public), GET /campaigns/public(public,active only)
FE: CampaignManagementPage.tsx — full API wired, FormData multipart, loading states, uuid keys, campaign isolation fix(user?.id dep in useEffect)
CRITICAL_BUG_FIXED: PG CTE snapshot isolation — INSERT in CTE invisible to SELECT on same table; fix=RETURNING * then SELECT FROM CTE alias not base table
FIELD_NAME_BUG: backend Campaign used charityId/endDate/totalRaised camelCase but frontend expected charity_id/end_date/total_raised snake_case → fixed in domain.ts+mapCampaign
NEW repo methods: addCampaign,getCampaignByUuid,getCampaignImage,listCampaignsByCharity,updateCampaign,closeCampaign,getCharityById
campaign.service.ts line ref: resolveManageableCharity L21, requireApproved L34, detectImageMime L14, processUploadedImage L55

## FR13_SEARCH(PR open feat/FR13-Listings-Filter)
BE listing.service.ts searchPublicListings extended: +condition(allowlist),+price_min/max(numeric validated),+campaign_id,+end_before(ISO),+sort(allowlist:ending_soon/newest/price_low/price_high)
SFR: listActiveListings()→WHERE status='active' only — draft/pending/rejected/cancelled/expired/sold NEVER returned regardless of params
isSafeSearchQuery(q,category): SQL_META regex rejects SELECT/DROP/UNION/OR 1=1/--/;/'/"/` → 400 UNSAFE_SEARCH_QUERY
FE AuctionsPage.tsx: full filter sidebar(category,condition,campaign dropdown from /campaigns/public,price min/max,end_before presets,sort dropdown), 250ms debounce, loading spinner, result count, clear all
SECRET_LEAK: cookies.txt+headers.txt from curl testing accidentally committed in 9c2d972 → removed in 737081b but still in history → rebase -i needed to rewrite; STATUS=in_progress

## BUGS_FIXED
1. husky pre-commit: `node ./node_modules/.bin/lint-staged` → `npx lint-staged` (.husky/pre-commit)
2. eslint tsconfigRootDir: cross-workspace lint-staged ran eslint on BE+FE files together → both eslint.config.js now have parserOptions.tsconfigRootDir=__dirname/__meta.dirname
3. react-hooks/set-state-in-effect: setCampaigns([]) moved inside async load() not in effect body directly
4. updateUser SQL: PR#12(contact_number)+PR#14(charity_id/last_login_at) conflicts resolved; merged SQL has all 3 new cols at $5,$12,$13
5. findUserByUsername duplicate: both PR#12+PR#14 added it; deduped in postgres.repository.ts+index.ts+repository.types.ts
6. App.tsx /charity/campaigns route dropped during PR#21 merge → restored in bug/restore-campaign-route PR#22

## MERGE_CONFLICTS_RESOLVED
PR#12(FR3 contact_number) vs PR#14(FR05): schema.sql, domain.ts, postgres.repository.ts, index.ts, repository.types.ts
PR#20(FR06) vs main(PR#21 FR08): CampaignManagementPage.tsx JSX conflicts — kept our backend-wired version(loading states,uuid keys,closingId,security infocard), took their scrollable modal structure(overflow-y-auto)
PR#14 pull main conflict: updateUser SET clause — resolved to include all: contact_number=$5,roles=$6,password_hash=$7...locked_until=$11,charity_id=$12,last_login_at=$13
profile.service.ts: user.contact_number→user.contactNumber(camelCase to match unified User type)

## CURRENT_BRANCHES
main=clean with FR03/05/06/08/bug fixes merged; feat/FR11-Realtime-Bidding=open PR#23 pending review(2 issues: socket localhost fallback,double bid count); feat/FR13-Listings-Filter=PR open secret scan failing(history rewrite pending)

## KNOWN_ISSUES
- AdminPage.tsx="Coming soon" — listing approval via SQL: UPDATE listings SET status='active',start_time=NOW(),end_time=NOW()+INTERVAL'3h' WHERE status='pending'
- FR11 reviewer issues: AuctionDetailPage.tsx L157 socket fallback 'http://localhost:5000' breaks AWS; bid count increments twice(API response+socket event)
- npm test wipes DB → always npm run seed after; test DB shares same PG instance as dev
- .vscode/settings.json tracked by git(committed in PR#12) — no password but flagged; fix=git rm --cached .vscode/settings.json

## KEY_FILES_LINE_REFS
rbac.middleware.ts:6=requireRole; csrf.middleware.ts:10=sha256 CSRF validation; securityHeaders.middleware.ts:4-8=5 security headers; auth.service.ts:15=ARGON2_OPTIONS,21=GENERIC_REG_MSG,56=hash,130-131=lockout; security.ts:58=SQL_META,60=isSafeSearchQuery,67=escapeHtml,83=sanitizeText; listing.service.ts:9=LOCKED_FIELDS,128=ALLOWED_SORTS,134=isSafeSearchQuery applied; postgres.repository.ts:697=WHERE status='active',728=appendAuditEvent hash chain,732=previousHash/GENESIS

## OWASP_PC2024_MAPPED
C1=rbac.middleware:6+charityStaff:20+listing:9(LOCKED_FIELDS); C2=auth:15(argon2id)+csrf:10(sha256)+pg.repo:732(hash chain); C3=security:83(sanitizeText)+60(isSafeSearch)+campaign:14(magic bytes); C4=SFRs per FR+app.ts global middleware; C5=secHeaders:4-8+listing:132(pending default); C6=argon2/rate-limit/eslint-security; C7=auth:21(enum prevention)+16-17(lockout)+OTP; C8=secHeaders:4-8(CSP/DENY/nosniff); C9=pg.repo:728(tamper-evident audit chain); C10=multer memoryStorage+BYTEA no outbound URL fetch
